package com.nicolas.forohub.dto;

public record TopicoDatos(
                Long id,
                String titulo,
                String mensaje,
                String curso,
                String creado,
                String nombre) {
}
